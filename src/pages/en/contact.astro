---
import Layout from "../../layouts/Layout.astro";
import Breadcrumb from "../../components/Breadcrumb.astro";
import BreadcrumbSchema from "../../components/BreadcrumbSchema.astro";

// SEO
const seo = {
    title: "Contact Us | AIU Cedar Society",
    description:
        "Contact AIU Cedar Society. For lecture inquiries, questions, or any other matters, please feel free to reach out.",
    keywords: "AIU Cedar Society,Contact,Inquiry,Get in Touch",
};

const breadcrumbItems = [{ label: "Home", href: "/en" }, { label: "Contact" }];
---

<Layout seo={seo}>
    <BreadcrumbSchema items={breadcrumbItems} />
    <div class="min-h-screen">
        <div
            class="container mx-auto px-4 md:px-6 pt-16 md:pt-20 pb-12 md:pb-20 max-w-5xl"
        >
            <Breadcrumb items={breadcrumbItems} />
            {/* Back Button */}
            <a
                href="/en"
                class="inline-flex items-center text-gray-500 hover:text-[#006837] mb-8 md:mb-12 transition-colors group text-sm font-medium min-h-[44px]"
            >
                <svg
                    class="w-4 h-4 mr-2 transform group-hover:-translate-x-1 transition-transform"
                    fill="none"
                    viewBox="0 0 24 24"
                    stroke="currentColor"
                >
                    <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        stroke-width="2"
                        d="M15 19l-7-7 7-7"></path>
                </svg>
                Back to Home
            </a>

            {/* Hero Section */}
            <div class="relative mb-12 md:mb-16">
                <div class="relative mb-6">
                    <div class="absolute top-0 left-0 w-px h-full bg-[#006837]">
                    </div>
                    <h1
                        class="typography-hero pl-6"
                    >
                        Contact <span class="text-[#006837] typography-hero-accent">Us</span>
                    </h1>
                </div>
                <p
                    class="text-lg md:text-xl text-gray-600 leading-relaxed pl-6 max-w-2xl"
                >
                    If you have any questions or inquiries, please feel free to
                    contact us.
                </p>
            </div>

            {/* Contact Form Card */}
            <div class="bg-white rounded-lg shadow-md p-6 md:p-10">
                <div
                    class="flex items-center gap-3 mb-8 pb-6 border-b border-gray-200"
                >
                    <div
                        class="w-6 h-6 bg-[#006837] bg-opacity-5 rounded-lg flex items-center justify-center flex-shrink-0"
                    >
                        <svg
                            class="w-4 h-4 text-[#006837]"
                            fill="none"
                            viewBox="0 0 24 24"
                            stroke="currentColor"
                        >
                            <path
                                stroke-linecap="round"
                                stroke-linejoin="round"
                                stroke-width="2"
                                d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"
                            ></path>
                        </svg>
                    </div>
                    <h2
                        class="text-xl md:text-2xl font-bold text-gray-900 tracking-tight"
                    >
                        Send a Message
                    </h2>
                </div>

                <form
                    id="contact-form"
                    action="https://formspree.io/f/xrbnkjrp"
                    method="POST"
                    class="space-y-6"
                    novalidate
                >
                    {/* Hidden field for language */}
                    <input type="hidden" name="language" value="en" />

                    {/* Name Field */}
                    <div>
                        <label
                            for="name"
                            class="block text-sm font-bold text-gray-700 mb-2"
                        >
                            Name <span class="text-red-500">*</span>
                        </label>
                        <input
                            type="text"
                            id="name"
                            name="name"
                            required
                            aria-required="true"
                            autocomplete="name"
                            class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-[#006837] focus:border-transparent transition-all outline-none"
                            placeholder="John Smith"
                        />
                        <p
                            class="text-sm text-red-600 mt-2 hidden"
                            data-error-for="name"
                        ></p>
                    </div>

                    {/* Email Field */}
                    <div>
                        <label
                            for="email"
                            class="block text-sm font-bold text-gray-700 mb-2"
                        >
                            Email <span class="text-red-500">*</span>
                        </label>
                        <input
                            type="email"
                            id="email"
                            name="email"
                            required
                            aria-required="true"
                            autocomplete="email"
                            class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-[#006837] focus:border-transparent transition-all outline-none"
                            placeholder="example@email.com"
                        />
                        <p
                            class="text-sm text-red-600 mt-2 hidden"
                            data-error-for="email"
                        ></p>
                    </div>

                    {/* Subject Field */}
                    <div>
                        <label
                            for="subject"
                            class="block text-sm font-bold text-gray-700 mb-2"
                        >
                            Subject <span class="text-red-500">*</span>
                        </label>
                        <input
                            type="text"
                            id="subject"
                            name="subject"
                            required
                            aria-required="true"
                            class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-[#006837] focus:border-transparent transition-all outline-none"
                            placeholder="About lectures"
                        />
                        <p
                            class="text-sm text-red-600 mt-2 hidden"
                            data-error-for="subject"
                        ></p>
                    </div>

                    {/* Message Field */}
                    <div>
                        <label
                            for="message"
                            class="block text-sm font-bold text-gray-700 mb-2"
                        >
                            Message <span class="text-red-500">*</span>
                        </label>
                        <textarea
                            id="message"
                            name="message"
                            rows="6"
                            required
                            aria-required="true"
                            class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-[#006837] focus:border-transparent transition-all outline-none resize-none"
                            placeholder="Please enter your message"></textarea>
                        <p
                            class="text-sm text-red-600 mt-2 hidden"
                            data-error-for="message"
                        ></p>
                    </div>

                    {/* Honeypot for spam protection */}
                    <input type="text" name="_gotcha" style="display:none" />

                    {/* Submit Button */}
                    <div class="pt-4">
                        <button
                            type="submit"
                            class="w-full bg-[#006837] text-white font-bold py-4 px-6 rounded-lg hover:bg-[#005028] transition-colors duration-300 shadow-md hover:shadow-lg transform hover:-translate-y-0.5 transition-transform min-h-[44px] cursor-pointer flex items-center justify-center gap-2"
                            data-submit-button
                        >
                            <span data-submit-text>Submit</span>
                            <svg
                                class="w-5 h-5 animate-spin text-white hidden"
                                data-submit-spinner
                                xmlns="http://www.w3.org/2000/svg"
                                fill="none"
                                viewBox="0 0 24 24"
                            >
                                <circle
                                    class="opacity-25"
                                    cx="12"
                                    cy="12"
                                    r="10"
                                    stroke="currentColor"
                                    stroke-width="4"
                                ></circle>
                                <path
                                    class="opacity-75"
                                    fill="currentColor"
                                    d="M4 12a8 8 0 018-8v4a4 4 0 00-4 4H4z"
                                ></path>
                            </svg>
                        </button>
                    </div>

                    {/* Privacy Notice */}
                    <p class="text-xs text-gray-500 text-center pt-2">
                        Your information will only be used to respond to your
                        inquiry.
                    </p>
                    <div
                        id="form-status"
                        class="text-sm text-gray-600 text-center pt-3 min-h-[1.5rem]"
                        role="status"
                        aria-live="polite"
                    ></div>
                </form>
            </div>

            {/* Additional Info Card */}
            <div
                class="mt-8 bg-gray-50 rounded-lg p-6 md:p-8 border border-gray-200"
            >
                <h3
                    class="text-lg font-bold text-gray-900 mb-4 flex items-center gap-2"
                >
                    <svg
                        class="w-5 h-5 text-[#006837]"
                        fill="none"
                        viewBox="0 0 24 24"
                        stroke="currentColor"
                    >
                        <path
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            stroke-width="2"
                            d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"
                        ></path>
                    </svg>
                    About Inquiries
                </h3>
                <div class="space-y-2 text-sm text-gray-600">
                    <p>• We typically respond within 2-3 business days</p>
                    <p>
                        • Lecture registration inquiries are also accepted
                        through this form
                    </p>
                    <p>
                        • For urgent matters, please include "[URGENT]" in the
                        subject line
                    </p>
                </div>
            </div>
        </div>
    </div>
</Layout>

<script is:inline>
    // Contact Form (EN): prevent redirect by submitting via fetch to Formspree
    (function () {
        const emailPattern = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;

        function initContactForm() {
            const form = document.getElementById("contact-form");
            if (!form) return;

            // Avoid double-binding if init runs multiple times
            if (form.dataset.enhanced === "true") return;
            form.dataset.enhanced = "true";

            const statusEl = document.getElementById("form-status");
            const submitBtn = form.querySelector("[data-submit-button]");
            const submitText = form.querySelector("[data-submit-text]");
            const submitSpinner = form.querySelector("[data-submit-spinner]");

            function setLoading(isLoading) {
                if (!submitBtn || !submitText || !submitSpinner) return;
                submitBtn.disabled = isLoading;
                submitBtn.classList.toggle("opacity-70", isLoading);
                submitSpinner.classList.toggle("hidden", !isLoading);
                submitText.textContent = isLoading ? "Submitting..." : "Submit";
            }

                function showCompletionModal(title, message, type = "success") {
                    try {
                        if (typeof window.showFormModal === "function") {
                            window.showFormModal(title, message, type);
                            return;
                        }
                    } catch (e) {
                        // fall through to DOM fallback
                    }

                    const modal = document.getElementById("form-modal");
                    const titleEl = document.getElementById("form-modal-title");
                    const messageEl =
                        document.getElementById("form-modal-message");
                    if (!modal || !titleEl || !messageEl) return;

                    modal.setAttribute("data-type", type);
                    titleEl.textContent = title;
                    messageEl.textContent = message;
                    modal.classList.remove("hidden");
                    modal.setAttribute("aria-hidden", "false");
                    document.body.classList.add("form-modal-open");
                }

            function showError(input, message) {
                const errorEl = form.querySelector(
                    `[data-error-for="${input.name}"]`,
                );
                input.setAttribute("aria-invalid", "true");
                if (errorEl) {
                    errorEl.textContent = message;
                    errorEl.classList.remove("hidden");
                }
            }

            function clearError(input) {
                const errorEl = form.querySelector(
                    `[data-error-for="${input.name}"]`,
                );
                input.removeAttribute("aria-invalid");
                if (errorEl) {
                    errorEl.textContent = "";
                    errorEl.classList.add("hidden");
                }
            }

            function validate() {
                let valid = true;
                const elements = Array.from(
                    form.querySelectorAll("input[required], textarea[required]"),
                );
                elements.forEach((el) => {
                    clearError(el);
                    if (!el.value.trim()) {
                        showError(el, "This field is required.");
                        valid = false;
                        return;
                    }
                    if (el.type === "email" && !emailPattern.test(el.value)) {
                        showError(el, "Please enter a valid email address.");
                        valid = false;
                    }
                });
                return valid;
            }

            form.addEventListener("input", (e) => {
                const target = e.target;
                if (
                    !(
                        target instanceof HTMLInputElement ||
                        target instanceof HTMLTextAreaElement
                    )
                )
                    return;
                clearError(target);
            });

            form.addEventListener("submit", async (e) => {
                e.preventDefault();
                if (!validate()) {
                    if (statusEl) {
                        statusEl.textContent = "Please check your input.";
                        statusEl.classList.remove("text-green-700");
                        statusEl.classList.add("text-red-600");
                    }
                    return;
                }

                setLoading(true);
                if (statusEl) {
                    statusEl.textContent = "Submitting...";
                    statusEl.classList.remove("text-red-600");
                    statusEl.classList.add("text-gray-600");
                }

                const formData = new FormData(form);
                try {
                    const res = await fetch(form.action, {
                        method: "POST",
                        body: formData,
                        headers: { Accept: "application/json" },
                    });

                    if (res.ok) {
                        form.reset();
                        if (statusEl) {
                            statusEl.textContent =
                                "Submitted. Thank you for contacting us.";
                            statusEl.classList.remove("text-red-600");
                            statusEl.classList.add("text-green-700");
                        }
                        if (window.showToast) {
                            window.showToast(
                                "Submitted. Thank you for contacting us.",
                                "success",
                            );
                        }
                        showCompletionModal(
                            "Submission complete",
                            "We received your message. We typically respond within 2–3 business days.",
                            "success",
                        );
                        // 🎉 Celebration confetti
                        if (typeof window.showConfetti === "function") {
                            window.showConfetti();
                        }
                    } else {
                        throw new Error("Submission failed.");
                    }
                } catch (error) {
                    console.error(error);
                    if (statusEl) {
                        statusEl.textContent =
                            "Submission failed. Please try again.";
                        statusEl.classList.remove("text-green-700");
                        statusEl.classList.add("text-red-600");
                    }
                    if (window.showToast) {
                        window.showToast(
                            "Submission failed. Please try again.",
                            "error",
                        );
                    }
                } finally {
                    setLoading(false);
                }
            });
        }

        document.addEventListener("DOMContentLoaded", initContactForm);
        document.addEventListener("astro:page-load", initContactForm);
    })();
</script>
