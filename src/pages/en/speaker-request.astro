---
import Layout from "../../layouts/Layout.astro";
import Breadcrumb from "../../components/Breadcrumb.astro";
import BreadcrumbSchema from "../../components/BreadcrumbSchema.astro";

// SEO
const seo = {
    title: "Speaker Request | AIU Cedar Society",
    description:
        "Request a speaker for AIU Cedar Society lecture series. Your suggestions help us plan future lectures.",
    keywords:
        "AIU Cedar Society,Speaker Request,Lecture,Guest Speaker,Suggestions",
};

const breadcrumbItems = [
    { label: "Home", href: "/en" },
    { label: "Speaker Request" },
];
---

<Layout seo={seo}>
    <BreadcrumbSchema items={breadcrumbItems} />
    <div class="min-h-screen">
        <div
            class="container mx-auto px-4 md:px-6 pt-16 md:pt-20 pb-12 md:pb-20 max-w-5xl"
        >
            <Breadcrumb items={breadcrumbItems} />
            {/* Back Button */}
            <a
                href="/en"
                class="inline-flex items-center text-gray-500 hover:text-[#006837] mb-8 md:mb-12 transition-colors group text-sm font-medium min-h-[44px]"
            >
                <svg
                    class="w-4 h-4 mr-2 transform group-hover:-translate-x-1 transition-transform"
                    fill="none"
                    viewBox="0 0 24 24"
                    stroke="currentColor"
                >
                    <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        stroke-width="2"
                        d="M15 19l-7-7 7-7"></path>
                </svg>
                Back to Home
            </a>

            {/* Hero Section */}
            <div class="relative mb-12 md:mb-16">
                <div class="relative mb-6">
                    <div class="absolute top-0 left-0 w-px h-full bg-[#006837]">
                    </div>
                    <h1 class="typography-hero pl-6">
                        Speaker <span class="text-[#006837] typography-hero-accent">Request</span>
                    </h1>
                </div>
                <p
                    class="text-lg md:text-xl text-gray-600 leading-relaxed pl-6 max-w-2xl"
                >
                    Tell us who you'd like to hear from! Your suggestions help
                    us plan future lectures.
                </p>
            </div>

            {/* Request Form Card */}
            <div class="bg-white rounded-lg shadow-md p-6 md:p-10">
                <div
                    class="flex items-center gap-3 mb-8 pb-6 border-b border-gray-200"
                >
                    <div
                        class="w-6 h-6 bg-[#006837] bg-opacity-5 rounded-lg flex items-center justify-center flex-shrink-0"
                    >
                        <svg
                            class="w-4 h-4 text-[#006837]"
                            fill="none"
                            viewBox="0 0 24 24"
                            stroke="currentColor"
                        >
                            <path
                                stroke-linecap="round"
                                stroke-linejoin="round"
                                stroke-width="2"
                                d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"
                            ></path>
                        </svg>
                    </div>
                    <h2
                        class="text-xl md:text-2xl font-bold text-gray-900 tracking-tight"
                    >
                        Submit a Request
                    </h2>
                </div>

                <form
                    id="speaker-request-form"
                    action="https://formspree.io/f/xrbnkjrp"
                    method="POST"
                    class="space-y-6"
                    novalidate
                >
                    {/* Hidden fields */}
                    <input
                        type="hidden"
                        name="_subject"
                        value="[Speaker Request] New Request Received"
                    />
                    <input
                        type="hidden"
                        name="form_type"
                        value="speaker_request"
                    />
                    <input type="hidden" name="language" value="en" />

                    {/* Speaker Name Field */}
                    <div>
                        <label
                            for="speaker"
                            class="block text-sm font-bold text-gray-700 mb-2"
                        >
                            Requested Speaker <span class="text-red-500">*</span
                            >
                        </label>
                        <input
                            type="text"
                            id="speaker"
                            name="speaker"
                            required
                            aria-required="true"
                            autocomplete="organization"
                            class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-[#006837] focus:border-transparent transition-all outline-none"
                            placeholder="Speaker's name, title, and affiliation"
                        />
                        <p
                            class="text-sm text-red-600 mt-2 hidden"
                            data-error-for="speaker"
                        ></p>
                        <p class="mt-2 text-xs text-gray-500">
                            e.g., Professor John Smith from XYZ University, CEO
                            Jane Doe of ABC Company
                        </p>
                    </div>

                    {/* Reason Field */}
                    <div>
                        <label
                            for="reason"
                            class="block text-sm font-bold text-gray-700 mb-2"
                        >
                            Reason <span class="text-red-500">*</span>
                        </label>
                        <textarea
                            id="reason"
                            name="reason"
                            rows="4"
                            required
                            aria-required="true"
                            class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-[#006837] focus:border-transparent transition-all outline-none resize-none"
                            placeholder="Why would you like to hear from this person?"
                        ></textarea>
                        <p
                            class="text-sm text-red-600 mt-2 hidden"
                            data-error-for="reason"
                        ></p>
                    </div>

                    {/* Topic Field */}
                    <div>
                        <label
                            for="topic"
                            class="block text-sm font-bold text-gray-700 mb-2"
                        >
                            Suggested Topic <span
                                class="text-gray-400 font-normal"
                                >(Optional)</span
                            >
                        </label>
                        <input
                            type="text"
                            id="topic"
                            name="topic"
                            class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-[#006837] focus:border-transparent transition-all outline-none"
                            placeholder="What topic would you like them to speak about?"
                        />
                    </div>

                    {/* Other Comments Field */}
                    <div>
                        <label
                            for="comments"
                            class="block text-sm font-bold text-gray-700 mb-2"
                        >
                            Other Comments <span
                                class="text-gray-400 font-normal"
                                >(Optional)</span
                            >
                        </label>
                        <textarea
                            id="comments"
                            name="comments"
                            rows="3"
                            class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-[#006837] focus:border-transparent transition-all outline-none resize-none"
                            placeholder="Any other requests or comments"
                        ></textarea>
                    </div>

                    {/* Honeypot for spam protection */}
                    <input type="text" name="_gotcha" style="display:none" />

                    {/* Submit Button */}
                    <div class="pt-4">
                        <button
                            type="submit"
                            class="w-full bg-[#006837] text-white font-bold py-4 px-6 rounded-lg hover:bg-[#005028] transition-colors duration-300 shadow-md hover:shadow-lg transform hover:-translate-y-0.5 transition-transform min-h-[44px] cursor-pointer flex items-center justify-center gap-2"
                            data-submit-button
                        >
                            <span data-submit-text>Submit Request</span>
                            <svg
                                class="w-5 h-5 animate-spin text-white hidden"
                                data-submit-spinner
                                xmlns="http://www.w3.org/2000/svg"
                                fill="none"
                                viewBox="0 0 24 24"
                            >
                                <circle
                                    class="opacity-25"
                                    cx="12"
                                    cy="12"
                                    r="10"
                                    stroke="currentColor"
                                    stroke-width="4"
                                ></circle>
                                <path
                                    class="opacity-75"
                                    fill="currentColor"
                                    d="M4 12a8 8 0 018-8v4a4 4 0 00-4 4H4z"
                                ></path>
                            </svg>
                        </button>
                    </div>

                    {/* Privacy Notice */}
                    <p class="text-xs text-gray-500 text-center pt-2">
                        Your information will only be used for lecture planning
                        purposes.
                    </p>
                    <div
                        id="form-status"
                        class="text-sm text-gray-600 text-center pt-3 min-h-[1.5rem]"
                        role="status"
                        aria-live="polite"
                    ></div>
                </form>
            </div>

            {/* Additional Info Card */}
            <div
                class="mt-8 bg-gray-50 rounded-lg p-6 md:p-8 border border-gray-200"
            >
                <h3
                    class="text-lg font-bold text-gray-900 mb-4 flex items-center gap-2"
                >
                    <svg
                        class="w-5 h-5 text-[#006837]"
                        fill="none"
                        viewBox="0 0 24 24"
                        stroke="currentColor"
                    >
                        <path
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            stroke-width="2"
                            d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"
                        ></path>
                    </svg>
                    About Requests
                </h3>
                <div class="space-y-2 text-sm text-gray-600">
                    <p>
                        • Your requests will be considered for future lecture
                        planning
                    </p>
                    <p>
                        • We may not be able to accommodate all requests, but we
                        value your input
                    </p>
                    <p>• We do not provide individual responses to requests</p>
                    <p>
                        • For lecture registration or general inquiries, please
                        use the <a
                            href="/en/contact"
                            class="text-[#006837] hover:underline"
                            >contact page</a
                        >
                    </p>
                </div>
            </div>
        </div>
    </div>
</Layout>

<script is:inline>
    // Speaker Request: validation + async submit + toast (parity with Contact UX)
    (function () {
        function initSpeakerRequestForm() {
            const form = document.getElementById("speaker-request-form");
            if (!form) return;

            const statusEl = document.getElementById("form-status");
            const submitBtn = form.querySelector("[data-submit-button]");
            const submitText = form.querySelector("[data-submit-text]");
            const submitSpinner = form.querySelector("[data-submit-spinner]");

            function setLoading(isLoading) {
                if (!submitBtn || !submitText || !submitSpinner) return;
                submitBtn.disabled = isLoading;
                submitBtn.classList.toggle("opacity-70", isLoading);
                submitSpinner.classList.toggle("hidden", !isLoading);
                submitText.textContent = isLoading
                    ? "Submitting..."
                    : "Submit Request";
            }

            function showCompletionModal(title, message, type = "success") {
                try {
                    if (typeof window.showFormModal === "function") {
                        window.showFormModal(title, message, type);
                        return;
                    }
                } catch (e) {
                    // fall through to DOM fallback
                }

                const modal = document.getElementById("form-modal");
                const titleEl = document.getElementById("form-modal-title");
                const messageEl =
                    document.getElementById("form-modal-message");
                if (!modal || !titleEl || !messageEl) return;

                modal.setAttribute("data-type", type);
                titleEl.textContent = title;
                messageEl.textContent = message;
                modal.classList.remove("hidden");
                modal.setAttribute("aria-hidden", "false");
                document.body.classList.add("form-modal-open");
            }

            function showError(input, message) {
                const errorEl = form.querySelector(
                    `[data-error-for="${input.name}"]`,
                );
                input.setAttribute("aria-invalid", "true");
                if (errorEl) {
                    errorEl.textContent = message;
                    errorEl.classList.remove("hidden");
                }
            }

            function clearError(input) {
                const errorEl = form.querySelector(
                    `[data-error-for="${input.name}"]`,
                );
                input.removeAttribute("aria-invalid");
                if (errorEl) {
                    errorEl.textContent = "";
                    errorEl.classList.add("hidden");
                }
            }

            function validate() {
                let valid = true;
                const requiredEls = Array.from(
                    form.querySelectorAll("input[required], textarea[required]"),
                );
                requiredEls.forEach((el) => {
                    clearError(el);
                    if (!el.value.trim()) {
                        showError(el, "This field is required.");
                        valid = false;
                    }
                });
                return valid;
            }

            form.addEventListener("input", (e) => {
                const target = e.target;
                if (
                    !(
                        target instanceof HTMLInputElement ||
                        target instanceof HTMLTextAreaElement
                    )
                )
                    return;
                clearError(target);
            });

            form.addEventListener("submit", async (e) => {
                e.preventDefault();
                if (!validate()) {
                    if (statusEl) {
                        statusEl.textContent = "Please check your input.";
                        statusEl.classList.remove("text-green-700");
                        statusEl.classList.add("text-red-600");
                    }
                    return;
                }

                setLoading(true);
                if (statusEl) {
                    statusEl.textContent = "Submitting...";
                    statusEl.classList.remove("text-red-600");
                    statusEl.classList.add("text-gray-600");
                }

                const formData = new FormData(form);
                try {
                    const res = await fetch(form.action, {
                        method: "POST",
                        body: formData,
                        headers: { Accept: "application/json" },
                    });

                    if (res.ok) {
                        form.reset();
                        if (statusEl) {
                            statusEl.textContent =
                                "Submitted. Thank you for your request!";
                            statusEl.classList.remove("text-red-600");
                            statusEl.classList.add("text-green-700");
                        }
                        if (window.showToast) {
                            window.showToast(
                                "Submitted. Thank you for your request!",
                                "success",
                            );
                        }
                        showCompletionModal(
                            "Submission complete",
                            "We received your speaker request. Thank you for your suggestion!",
                            "success",
                        );
                        // 🎉 Celebration confetti
                        if (typeof window.showConfetti === "function") {
                            window.showConfetti();
                        }
                    } else {
                        throw new Error("Submission failed.");
                    }
                } catch (error) {
                    console.error(error);
                    if (statusEl) {
                        statusEl.textContent =
                            "Submission failed. Please try again.";
                        statusEl.classList.remove("text-green-700");
                        statusEl.classList.add("text-red-600");
                    }
                    if (window.showToast) {
                        window.showToast(
                            "Submission failed. Please try again.",
                            "error",
                        );
                    }
                } finally {
                    setLoading(false);
                }
            });
        }

        document.addEventListener("DOMContentLoaded", initSpeakerRequestForm);
        document.addEventListener("astro:page-load", initSpeakerRequestForm);
    })();
</script>
