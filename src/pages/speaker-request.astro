---
import Layout from "../layouts/Layout.astro";
import Breadcrumb from "../components/Breadcrumb.astro";
import BreadcrumbSchema from "../components/BreadcrumbSchema.astro";

// SEO
const seo = {
    title: "講演者リクエスト | AIU Cedar Society",
    description:
        "AIU Cedar Societyの講演会に呼んでほしい講演者のリクエストをお寄せください。皆様のご意見をもとに、今後の講演会企画に活かしてまいります。",
    keywords:
        "AIU Cedar Society,講演者リクエスト,講演会,講師依頼,ゲストスピーカー",
};

const breadcrumbItems = [
    { label: "ホーム", href: "/" },
    { label: "講演者リクエスト" },
];
---

<Layout seo={seo}>
    <BreadcrumbSchema items={breadcrumbItems} />
    <div class="bg-gradient-to-b from-gray-50 to-white min-h-screen">
        <div
            class="container mx-auto px-4 md:px-6 pt-16 md:pt-20 pb-12 md:pb-20 max-w-5xl"
        >
            <Breadcrumb items={breadcrumbItems} />
            {/* Back Button */}
            <a
                href="/"
                class="inline-flex items-center text-gray-500 hover:text-[#006837] mb-8 md:mb-12 transition-colors group text-sm font-medium min-h-[44px]"
            >
                <svg
                    class="w-4 h-4 mr-2 transform group-hover:-translate-x-1 transition-transform"
                    fill="none"
                    viewBox="0 0 24 24"
                    stroke="currentColor"
                >
                    <path
                        stroke-linecap="round"
                        stroke-linejoin="round"
                        stroke-width="2"
                        d="M15 19l-7-7 7-7"></path>
                </svg>
                ホームへ
            </a>

            {/* Hero Section */}
            <div class="relative mb-12 md:mb-16" data-animate>
                <div class="relative mb-6">
                    <div class="absolute top-0 left-0 w-px h-full bg-[#006837]">
                    </div>
                    <h1
                        class="text-4xl md:text-5xl lg:text-6xl font-bold text-gray-900 tracking-tight leading-tight pl-6"
                    >
                        講演者リクエスト
                    </h1>
                </div>
                <p
                    class="text-lg md:text-xl text-gray-600 leading-relaxed pl-6 max-w-2xl"
                >
                    「この人の話を聞きたい！」という講演者のリクエストをお寄せください。皆様のご意見を参考に、今後の講演会を企画いたします。
                </p>
            </div>

            {/* Request Form Card */}
            <div
                class="bg-white rounded-lg shadow-md p-6 md:p-10"
                data-animate
                data-animate-delay="1"
            >
                <div
                    class="flex items-center gap-3 mb-8 pb-6 border-b border-gray-200"
                >
                    <div
                        class="w-6 h-6 bg-[#006837] bg-opacity-5 rounded-lg flex items-center justify-center flex-shrink-0"
                    >
                        <svg
                            class="w-4 h-4 text-[#006837]"
                            fill="none"
                            viewBox="0 0 24 24"
                            stroke="currentColor"
                        >
                            <path
                                stroke-linecap="round"
                                stroke-linejoin="round"
                                stroke-width="2"
                                d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"
                            ></path>
                        </svg>
                    </div>
                    <h2
                        class="text-xl md:text-2xl font-bold text-gray-900 tracking-tight"
                    >
                        リクエストを送信
                    </h2>
                </div>

                <form
                    id="speaker-request-form"
                    action="https://formspree.io/f/xrbnkjrp"
                    method="POST"
                    class="space-y-6"
                    novalidate
                >
                    {/* Hidden field to identify this as a speaker request */}
                    <input
                        type="hidden"
                        name="_subject"
                        value="【講演者リクエスト】新規リクエストが届きました"
                    />
                    <input
                        type="hidden"
                        name="form_type"
                        value="speaker_request"
                    />

                    {/* Speaker Name Field */}
                    <div>
                        <label
                            for="speaker"
                            class="block text-sm font-bold text-gray-700 mb-2"
                        >
                            希望する講演者 <span class="text-red-500">*</span>
                        </label>
                        <input
                            type="text"
                            id="speaker"
                            name="speaker"
                            required
                            aria-required="true"
                            autocomplete="organization"
                            class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-[#006837] focus:border-transparent transition-all outline-none"
                            placeholder="講演者のお名前、肩書き、所属など"
                        />
                        <p
                            class="text-sm text-red-600 mt-2 hidden"
                            data-error-for="speaker"
                        ></p>
                        <p class="mt-2 text-xs text-gray-500">
                            例: 〇〇大学教授 山田太郎氏、株式会社〇〇 CEO
                            佐藤花子氏
                        </p>
                    </div>

                    {/* Reason Field */}
                    <div>
                        <label
                            for="reason"
                            class="block text-sm font-bold text-gray-700 mb-2"
                        >
                            理由・背景 <span class="text-red-500">*</span>
                        </label>
                        <textarea
                            id="reason"
                            name="reason"
                            rows="4"
                            required
                            aria-required="true"
                            class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-[#006837] focus:border-transparent transition-all outline-none resize-none"
                            placeholder="なぜこの方の話を聞きたいのか、理由をお聞かせください"
                        ></textarea>
                        <p
                            class="text-sm text-red-600 mt-2 hidden"
                            data-error-for="reason"
                        ></p>
                    </div>

                    {/* Topic Field */}
                    <div>
                        <label
                            for="topic"
                            class="block text-sm font-bold text-gray-700 mb-2"
                        >
                            講演テーマ <span class="text-gray-400 font-normal"
                                >(任意)</span
                            >
                        </label>
                        <input
                            type="text"
                            id="topic"
                            name="topic"
                            class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-[#006837] focus:border-transparent transition-all outline-none"
                            placeholder="どのようなテーマでお話しいただきたいですか"
                        />
                    </div>

                    {/* Other Comments Field */}
                    <div>
                        <label
                            for="comments"
                            class="block text-sm font-bold text-gray-700 mb-2"
                        >
                            その他 <span class="text-gray-400 font-normal"
                                >(任意)</span
                            >
                        </label>
                        <textarea
                            id="comments"
                            name="comments"
                            rows="3"
                            class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-[#006837] focus:border-transparent transition-all outline-none resize-none"
                            placeholder="その他、ご要望やコメントがあればお聞かせください"
                        ></textarea>
                    </div>

                    {/* Honeypot for spam protection */}
                    <input type="text" name="_gotcha" style="display:none" />

                    {/* Submit Button */}
                    <div class="pt-4">
                        <button
                            type="submit"
                            class="w-full bg-[#006837] text-white font-bold py-4 px-6 rounded-lg hover:bg-[#005028] transition-colors duration-300 shadow-md hover:shadow-lg transform hover:-translate-y-0.5 transition-transform min-h-[44px] cursor-pointer flex items-center justify-center gap-2"
                            data-submit-button
                        >
                            <span data-submit-text>リクエストを送信</span>
                            <svg
                                class="w-5 h-5 animate-spin text-white hidden"
                                data-submit-spinner
                                xmlns="http://www.w3.org/2000/svg"
                                fill="none"
                                viewBox="0 0 24 24"
                            >
                                <circle
                                    class="opacity-25"
                                    cx="12"
                                    cy="12"
                                    r="10"
                                    stroke="currentColor"
                                    stroke-width="4"
                                ></circle>
                                <path
                                    class="opacity-75"
                                    fill="currentColor"
                                    d="M4 12a8 8 0 018-8v4a4 4 0 00-4 4H4z"
                                ></path>
                            </svg>
                        </button>
                    </div>

                    {/* Privacy Notice */}
                    <p class="text-xs text-gray-500 text-center pt-2">
                        送信いただいた情報は、講演会企画の参考のみに使用いたします。
                    </p>
                    <div
                        id="form-status"
                        class="text-sm text-gray-600 text-center pt-3 min-h-[1.5rem]"
                        role="status"
                        aria-live="polite"
                    ></div>
                </form>
            </div>

            {/* Additional Info Card */}
            <div
                class="mt-8 bg-gray-50 rounded-lg p-6 md:p-8 border border-gray-200"
            >
                <h3
                    class="text-lg font-bold text-gray-900 mb-4 flex items-center gap-2"
                >
                    <svg
                        class="w-5 h-5 text-[#006837]"
                        fill="none"
                        viewBox="0 0 24 24"
                        stroke="currentColor"
                    >
                        <path
                            stroke-linecap="round"
                            stroke-linejoin="round"
                            stroke-width="2"
                            d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"
                        ></path>
                    </svg>
                    リクエストについて
                </h3>
                <div class="space-y-2 text-sm text-gray-600">
                    <p>
                        •
                        いただいたリクエストは今後の講演会企画の参考にさせていただきます
                    </p>
                    <p>
                        •
                        全てのリクエストに対応できるわけではございませんが、貴重なご意見として検討いたします
                    </p>
                    <p>• リクエストに対する個別の返信は行っておりません</p>
                    <p>
                        • 講演会への参加申し込みや一般のお問い合わせは<a
                            href="/contact"
                            class="text-[#006837] hover:underline"
                            >お問い合わせページ</a
                        >をご利用ください
                    </p>
                </div>
            </div>
        </div>
    </div>
</Layout>

<script is:inline>
    // 講演者リクエスト：フォーム送信とバリデーションの強化（Contactと体験を統一）
    (function () {
        function initSpeakerRequestForm() {
            const form = document.getElementById("speaker-request-form");
            if (!form) return;

            const statusEl = document.getElementById("form-status");
            const submitBtn = form.querySelector("[data-submit-button]");
            const submitText = form.querySelector("[data-submit-text]");
            const submitSpinner = form.querySelector("[data-submit-spinner]");

            function setLoading(isLoading) {
                if (!submitBtn || !submitText || !submitSpinner) return;
                submitBtn.disabled = isLoading;
                submitBtn.classList.toggle("opacity-70", isLoading);
                submitSpinner.classList.toggle("hidden", !isLoading);
                submitText.textContent = isLoading
                    ? "送信中..."
                    : "リクエストを送信";
            }

            function showCompletionModal(title, message, type = "success") {
                try {
                    if (typeof window.showFormModal === "function") {
                        window.showFormModal(title, message, type);
                        return;
                    }
                } catch (e) {
                    // fall through to DOM fallback
                }
                const modal = document.getElementById("form-modal");
                const titleEl = document.getElementById("form-modal-title");
                const messageEl = document.getElementById("form-modal-message");
                if (!modal || !titleEl || !messageEl) return;
                modal.setAttribute("data-type", type);
                titleEl.textContent = title;
                messageEl.textContent = message;
                modal.classList.remove("hidden");
                modal.setAttribute("aria-hidden", "false");
                document.body.classList.add("form-modal-open");
            }

            function showError(input, message) {
                const errorEl = form.querySelector(
                    `[data-error-for="${input.name}"]`,
                );
                input.setAttribute("aria-invalid", "true");
                if (errorEl) {
                    errorEl.textContent = message;
                    errorEl.classList.remove("hidden");
                }
            }

            function clearError(input) {
                const errorEl = form.querySelector(
                    `[data-error-for="${input.name}"]`,
                );
                input.removeAttribute("aria-invalid");
                if (errorEl) {
                    errorEl.textContent = "";
                    errorEl.classList.add("hidden");
                }
            }

            function validate() {
                let valid = true;
                const requiredEls = Array.from(
                    form.querySelectorAll("input[required], textarea[required]"),
                );
                requiredEls.forEach((el) => {
                    clearError(el);
                    if (!el.value.trim()) {
                        showError(el, "必須項目です。");
                        valid = false;
                    }
                });
                return valid;
            }

            // リアルタイムでエラー解除
            form.addEventListener("input", (e) => {
                const target = e.target;
                if (
                    !(
                        target instanceof HTMLInputElement ||
                        target instanceof HTMLTextAreaElement
                    )
                )
                    return;
                clearError(target);
            });

            form.addEventListener("submit", async (e) => {
                e.preventDefault();
                if (!validate()) {
                    if (statusEl) {
                        statusEl.textContent = "入力内容を確認してください。";
                        statusEl.classList.remove("text-green-700");
                        statusEl.classList.add("text-red-600");
                    }
                    return;
                }

                setLoading(true);
                if (statusEl) {
                    statusEl.textContent = "送信中です...";
                    statusEl.classList.remove("text-red-600");
                    statusEl.classList.add("text-gray-600");
                }

                const formData = new FormData(form);
                try {
                    const res = await fetch(form.action, {
                        method: "POST",
                        body: formData,
                        headers: { Accept: "application/json" },
                    });

                    if (res.ok) {
                        form.reset();
                        if (statusEl) {
                            statusEl.textContent =
                                "送信しました。貴重なご意見ありがとうございます。";
                            statusEl.classList.remove("text-red-600");
                            statusEl.classList.add("text-green-700");
                        }
                        if (window.showToast) {
                            window.showToast(
                                "送信しました。貴重なご意見ありがとうございます。",
                                "success",
                            );
                        }
                        showCompletionModal(
                            "送信が完了しました",
                            "講演者リクエストを受け付けました。貴重なご意見ありがとうございます。",
                            "success",
                        );
                    } else {
                        throw new Error("送信に失敗しました。");
                    }
                } catch (error) {
                    console.error(error);
                    if (statusEl) {
                        statusEl.textContent =
                            "送信に失敗しました。お手数ですが再度お試しください。";
                        statusEl.classList.remove("text-green-700");
                        statusEl.classList.add("text-red-600");
                    }
                    if (window.showToast) {
                        window.showToast(
                            "送信に失敗しました。再度お試しください。",
                            "error",
                        );
                    }
                } finally {
                    setLoading(false);
                }
            });
        }

        // 初回/画面遷移(View Transitions)どちらでも確実に初期化
        document.addEventListener("DOMContentLoaded", initSpeakerRequestForm);
        document.addEventListener("astro:page-load", initSpeakerRequestForm);
    })();
</script>
